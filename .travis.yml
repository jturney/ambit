language: cpp

os:
  - linux

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - boost-latest
    packages:
#      - gdb
#      - gcc-4.9
#      - g++-4.9
#      - gfortran-4.9
      - liblapack-dev
      - libboost1.55-all-dev

script:
  - mkdir objdir && cd objdir && ../setup && make
